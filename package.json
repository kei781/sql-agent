{
  "name": "sql-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Safe Slack-driven natural-language-to-SQL bot MVP foundation",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test tests/unit/intent.test.ts tests/unit/sqlSafety.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "node-sql-parser": "5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
